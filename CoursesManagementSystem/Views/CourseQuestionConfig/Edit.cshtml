@model CoursesManagementSystem.DB.Models.CourseQuestionConfig

@{
    ViewData["Title"] = "Edit Course Question Configuration";
}

<h3 class="text-primary mb-4"><i class="bi bi-pencil-square"></i> Edit Course Question Configuration</h3>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="ID" />

    <div class="mb-3">
        <label asp-for="QuestionsCountPerLesson" class="form-label fw-bold">Questions Count Per Lesson</label>
        <input type="number" class="form-control" asp-for="QuestionsCountPerLesson">
        <span asp-validation-for="QuestionsCountPerLesson" class="text-danger"></span>
    </div>

  @*   <div class="mb-3">
        <label asp-for="CourseId" class="form-label fw-bold">Course <span class="text-danger">*</span></label>
        <select asp-for="CourseId" asp-items="ViewBag.Courses" class="form-control" required>
            <option value="">-- Select a Course --</option>
        </select>
        <span asp-validation-for="CourseId" class="text-danger"></span>
    </div> *@
    <input type="hidden" name="CourseId" value="@Model.CourseId" />


    <div class="mb-3">
        <label asp-for="QuestionLevelId" class="form-label fw-bold">Question Level <span class="text-danger">*</span></label>
        <select asp-for="QuestionLevelId" asp-items="ViewBag.QuestionLevels" class="form-control" required>
            <option value="">-- Select a Question Level --</option>
        </select>
        <span asp-validation-for="QuestionLevelId" class="text-danger"></span>
    </div>

    <div class="d-flex justify-content-between mt-4">
        <a class="btn btn-secondary" asp-controller="CourseQuestionConfig" asp-action="CourseQuestionConfigByCourseId" asp-route-Courseid="@Model.CourseId">Cancel</a>
        <button type="submit" class="btn btn-primary">Save Changes</button>
    </div>
</form>
